// require("dotenv").config();
// const { Telegraf } = require("telegraf");

// const bot = new Telegraf(process.env.BOT_TOKEN);

// // Define the main keyboard with all options
// const mainKeyboard = {
//   keyboard: [
//     [{ text: "🍀 Наши товары" }, { text: "💸 Оплата" }],
//     [{ text: "📍 Адрес" }, { text: "ℹ️ О нас" }],
//   ],
//   resize_keyboard: true, // Ensures buttons resize to fit the screen
//   one_time_keyboard: true, // Optional: hides the keyboard after a button is pressed
// };

// // Define the back keyboard with only the "Back" button
// const backToMain = {
//   keyboard: [[{ text: "🔙 Назад" }]],
//   resize_keyboard: true,
//   one_time_keyboard: true,
// };

// const productsKeyboard = {
//   keyboard: [
//     [{ text: "🐤 Детские товары" }, { text: "🧼 Гигиена" }],
//     [{ text: "💄 Косметика" }, { text: "🔙 Назад" }],
//   ],
//   resize_keyboard: true,
//   one_time_keyboard: true,
// };

// // Start command to display the main keyboard
// bot.start((ctx) => {
//   ctx.reply("Welcome! Choose an option:", {
//     reply_markup: mainKeyboard,
//   });
// });

// // Handle button clicks and switch to back keyboard
// bot.hears("🍀 Наши товары", (ctx) => {
//   ctx.reply("Here are our products!", {
//     reply_markup: productsKeyboard,
//   });
// });

// bot.hears("💸 Оплата", (ctx) => {
//   ctx.reply("Payment details coming up!", {
//     reply_markup: backToMain,
//   });
// });

// bot.hears("📍 Адрес", (ctx) => {
//   ctx.reply("Here is our address!", {
//     reply_markup: backToMain,
//   });
// });

// bot.hears("ℹ️ О нас", (ctx) => {
//   ctx.reply("Learn more about us!", {
//     reply_markup: backToMain,
//   });
// });

// // Handle the "Back" button to return to the main keyboard
// bot.hears("🔙 Назад", (ctx) => {
//   ctx.reply("Back to main menu:", {
//     reply_markup: mainKeyboard,
//   });
// });

// // Launch the bot
// bot.launch();

// // Enable graceful stop
// process.once("SIGINT", () => bot.stop("SIGINT"));
// process.once("SIGTERM", () => bot.stop("SIGTERM"));